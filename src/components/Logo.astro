<a href="/" class="logo nav-logo">
    <img src="/grainlogo.png" alt="Grain Logo" class="logo-image" />
</a>

<style>
    .logo {
        display: flex;
        align-items: center;
        text-decoration: none;
        /* color: white; */ /* Removed - Color will be inherited from parent (.nav-bar) */
        font-size: 2.5rem;
        font-weight: 900;
        font-style: italic;
        letter-spacing: -0.1em;
        position: relative;
        font-family: "Arial Black", Arial, sans-serif;
        transition: opacity 0.8s ease, transform 0.8s ease;
        z-index: 100;
    }

    .logo.hidden {
        opacity: 0;
        transform: translateY(-20px);
        pointer-events: none;
        visibility: hidden;
    }

    .logo-image {
        height: 2.5rem;
        width: auto;
        transition: all 0.3s ease;
    }

    .logo:hover .logo-image {
        animation: grainEffect 0.8s steps(10) infinite;
    }

    .logo-text:nth-child(1) { animation-delay: 0.1s; }
    .logo-text:nth-child(2) { animation-delay: 0.2s; }
    .logo-text:nth-child(3) { animation-delay: 0.3s; }
    .logo-text:nth-child(4) { animation-delay: 0.4s; }
    .logo-text:nth-child(5) { animation-delay: 0.5s; }

    @keyframes grainEffect {
        0% {
            transform: translate(0, 0);
            filter: brightness(1) contrast(1.1);
        }
        25% {
            transform: translate(-1px, 1px);
            filter: brightness(1.1) contrast(1.2);
        }
        50% {
            transform: translate(1px, -1px);
            filter: brightness(0.9) contrast(1.1);
        }
        75% {
            transform: translate(-1px, -1px);
            filter: brightness(1.05) contrast(1.15);
        }
        100% {
            transform: translate(0, 0);
            filter: brightness(1) contrast(1.1);
        }
    }

    /* Footer logo specific styles */
    :global(.footer-logo) {
        align-items: stretch !important;
        width: 100% !important;
    }

    :global(.footer-logo) .logo {
        width: 100% !important;
        justify-content: center !important;
        display: flex !important;
    }

    :global(.footer-logo) .logo-image {
        height: 10rem !important;
        width: auto !important;
        max-width: none !important;
        display: block !important;
        margin: 0 auto !important;
    }

    @media (max-width: 768px) {
        :global(.footer-logo) .logo {
            font-size: 2.5rem;
        }
        :global(.footer-logo) .logo-image {
            height: 7rem !important;
            max-width: 300px !important;
        }
    }
</style> 